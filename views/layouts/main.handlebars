<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header>
    <h1>
      Tech Blog
    </h1>
    <!-- Toggle the logout/login button based on if the user is logged in -->
    <nav>
          <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/api/dashboard">Dashboard</a></li>
          {{#if logged_in}}
            <li><button class="btn" id="logout">Logout</button></li>
          {{else}}
            <li><a href="/login" class="btn" id="login">Login</a></li>
          {{/if}}
          </ul>
    </nav>
  </header>
  <main>
    {{{ body }}}
  </main>
  <footer>
      Tidy Lines LLC
  </footer>

  <!-- Only include the logout script if the user is logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
